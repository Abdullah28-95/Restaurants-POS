'use client';
import {useEffect,useState} from 'react';
type Data={items:Array<{name:string;quantity:number;price:number}>;price:number;tax:number;discount:number;grandTotal:number;branch:string};
const empty:Data={items:[],price:0,tax:0,discount:0,grandTotal:0,branch:''};
export default function Display(){const [d,setD]=useState<Data>(empty);useEffect(()=>{try{const x=localStorage.getItem('futec-pos-display');if(x)setD(JSON.parse(x));const bc=new BroadcastChannel('futec-pos-display');bc.onmessage=e=>setD(e.data);return()=>bc.close()}catch{}},[]);return <main style={{minHeight:'100vh',background:'#101214',color:'white',padding:'4vw',display:'grid',gridTemplateRows:'auto 1fr auto',gap:24}}><header style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><div><div style={{fontSize:44,fontWeight:900}}>{d.branch||'Futec Restaurant'}</div><div style={{fontSize:22,color:'#aeb3bb'}}>طلبك الحالي</div></div><img src="/assets/logo.jpeg" style={{width:90,height:90,borderRadius:20,objectFit:'cover'}}/></header><section style={{overflow:'auto'}}>{d.items.length?d.items.map((x,i)=><div key={i} style={{display:'grid',gridTemplateColumns:'1fr auto auto',gap:30,borderBottom:'1px solid #30343a',padding:'18px 0',fontSize:28}}><b>{x.name}</b><span>× {x.quantity}</span><span className="num">{(x.price*x.quantity).toFixed(3)}</span></div>):<div style={{display:'grid',placeItems:'center',height:'100%',fontSize:38,color:'#6f7782'}}>بانتظار الطلب...</div>}</section><footer style={{display:'grid',gridTemplateColumns:'repeat(4,1fr)',gap:12}}>{[['المجموع',d.price],['الضريبة',d.tax],['الخصم',d.discount],['الإجمالي',d.grandTotal]].map(([k,v],i)=><div key={String(k)} style={{padding:22,borderRadius:20,background:i===3?'#d8a61d':'#22262b',color:i===3?'#17130b':'white'}}><div style={{fontSize:18,opacity:.75}}>{k}</div><div className="num" style={{fontSize:36,fontWeight:900,marginTop:8}}>{Number(v).toFixed(3)}</div></div>)}</footer></main>}
