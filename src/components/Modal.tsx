'use client';
export function Modal({open,title,onClose,children,actions,width}:{open:boolean;title:string;onClose:()=>void;children:React.ReactNode;actions?:React.ReactNode;width?:number}){if(!open)return null;return <div className="modal-backdrop" onMouseDown={e=>{if(e.currentTarget===e.target)onClose()}}><div className="modal" style={width?{width:`min(${width}px,96vw)`}:undefined}><div className="modal-head"><strong>{title}</strong><button className="btn ghost" onClick={onClose}>✕</button></div><div className="modal-body">{children}</div>{actions&&<div className="modal-actions">{actions}</div>}</div></div>}
